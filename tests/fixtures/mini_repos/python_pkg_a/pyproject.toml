[project]
name = "python-pkg-a"
version = "0.1.0"
description = "Python project"
requires-python = ">=3.10"
dependencies = [

]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pytest>=7.4.3",
    "black>=23.11.0",
    "ruff>=0.1.0",
]
